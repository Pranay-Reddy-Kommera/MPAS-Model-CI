#----------------------------------------------------------------------------
# Test case configuration for the 120km resolution
#
# To add a new test case at a different resolution:
#   1. Create .github/test-cases/<resolution>/config.env (copy this file)
#   2. Upload the test case archive to the NCAR/mpas-ci-data repo as <resolution>.tar.gz
#   3. Generate a reference log and save it as:
#      .github/test-cases/<resolution>/reference_log.atmosphere.0000.out
#   4. Add <resolution> to the 'resolution' matrix in your workflow
#----------------------------------------------------------------------------

# Model run duration (format: D_HH:MM:SS)
RUN_DURATION=0_02:00:00

# Restart output interval (format: D_HH:MM:SS)
RESTART_INTERVAL=0_02:00:00

# Run timeout in minutes
RUN_TIMEOUT=30

# Test data configuration
DATA_RESOLUTION=120km
DATA_REPO=NCAR/mpas-ci-data

