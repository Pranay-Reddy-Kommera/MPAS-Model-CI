# Subset CI: NVHPC + MPICH (CPU)
# Quick ECT validation with 4 MPI ranks.

name: "NVHPC+MPICH (CPU)"

on:
  workflow_dispatch:
  push:
    branches: [master, develop, 'hackathon-*', hack26, 'hack26/**']
  pull_request:
    branches: [master, develop, 'hackathon-*', hack26, 'hack26/**']

jobs:
  test:
    uses: ./.github/workflows/_test-compiler.yml
    with:
      compiler: nvhpc
      mpi: mpich

