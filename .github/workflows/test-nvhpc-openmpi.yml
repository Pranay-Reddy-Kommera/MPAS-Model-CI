# Subset CI: NVHPC + OpenMPI (CPU)
# Quick ECT validation with 4 MPI ranks.

name: "NVHPC+OpenMPI (CPU)"

on:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/_test-compiler.yml
    with:
      compiler: nvhpc
      mpi: openmpi
