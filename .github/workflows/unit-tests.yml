# Unit Tests (stub)
# Full implementation on feature-ci-test-cases branch.
name: Unit Tests

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Unit tests workflow placeholder — see feature-ci-test-cases branch for full implementation."
